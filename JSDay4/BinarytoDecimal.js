// Math.pow (x,y) = Math x to power of y
// pow is an inbuilt function
// Math.pow(2,3) = 2 power 3 i.e., 8

function Decimal(a^2) {
    let ans=0;
    while (a) {
        let rem = a % 10;
        ans = ans+ rem* Math.pow(2,power)
        power++;
        a = parseInt (a/10)
    } return ans;
} console.log (ans)