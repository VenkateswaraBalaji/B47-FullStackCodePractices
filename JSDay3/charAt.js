// charAt is a inbuilt which takeds index -> returns the character present in that index

// Example "Hello" -> charAt(2) returns 'l', charAt(4) returns 'o'

toUppercase() // converts to Uppercase
toLowercase() // converts to Lowercase

